apply plugin: 'idea'
description = 'Google Protocol Buffers'
dependencies {
    compile group: 'com.google.protobuf', name: 'protobuf-java', version: '3.4.0'
}

idea {
    module {
        sourceDirs += file("${projectDir}/proto3")
    }
}